#!/bin/sh /etc/rc.common

USE_PROCD=1
START=95

start_service() {
	[ -x /usr/sbin/unetic-core ] || return 0
	procd_open_instance
	procd_set_param command /usr/sbin/unetic-core
	procd_set_param respawn
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}
